import styled from "styled-components";

export const Container = styled.div`
    h1{
        color: var(--color-primary);
        font-size: 3rem;
        letter-spacing: 2px;
        text-align: center;
    }
`